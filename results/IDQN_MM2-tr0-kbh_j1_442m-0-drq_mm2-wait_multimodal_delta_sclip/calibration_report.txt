============================================================
  norm_wait / clip_wait calibration report
============================================================

Dataset : 56 step-deltas across 1 episodes

Per-step |Δ combined_wait| distribution:
  mean :        290.5
  std  :        178.3
  p50  :        291.8
  p75  :        443.1  ← recommended norm_wait
  p90  :        514.5
  p95  :        529.8
  p99  :        563.7

Mean accumulated wait per step:
  car  :       3393.5
  bike :       1236.8  (0.4x car)
  ped  :         53.6  (0.02x car)

--- Recommended mdp_configs entry ---

'IDQN_MM2': {
    # --- calibrated from run data ---
    'norm_wait'        : 443.1,
    'clip_wait'        : 3.0,

    # Option A: equal weights, larger norm  (simpler, try first)
    'car_wait_weight'  : 1.0,
    'bike_wait_weight' : 1.0,
    'ped_wait_weight'  : 1.0,

    # Option B: balanced weights so each mode contributes equally to reward
    # 'car_wait_weight'  : 1.0,
    # 'bike_wait_weight' : 2.7437,   # down-weights bike by 0.4x
    # 'ped_wait_weight'  : 63.3159,
},

--- What this fixes ---
Current norm_wait=224 → typical delta (443) / 224 = 2.0x clip limit.
Every reward is clipped to ±3.0; the agent sees only a binary signal.

New norm_wait=443.1 → typical delta normalises to ~1.0 before clipping.
clip_wait=3.0 keeps the ±4 headroom for outlier steps.

If you want to reduce bike dominance, use Option B weights.
Bike currently contributes 26% of combined wait;
balanced weights bring it to ~33%.

============================================================