============================================================
  norm_wait / clip_wait calibration report
============================================================

Dataset : 107,900 step-deltas across 100 episodes

Per-step |Δ combined_wait| distribution:
  mean :        383.4
  std  :        472.6
  p50  :        272.5
  p75  :        481.5  ← recommended norm_wait
  p90  :        715.5
  p95  :       1186.5
  p99  :       2456.0

Mean accumulated wait per step:
  car  :       2700.8
  bike :       3282.4  (1.2x car)
  ped  :         10.8  (0.00x car)

--- Recommended mdp_configs entry ---

'IDQN_MM2': {
    # --- calibrated from run data ---
    'norm_wait'        : 481.5,
    'clip_wait'        : 3.0,

    # Option A: equal weights, larger norm  (simpler, try first)
    'car_wait_weight'  : 1.0,
    'bike_wait_weight' : 1.0,
    'ped_wait_weight'  : 1.0,

    # Option B: balanced weights so each mode contributes equally to reward
    # 'car_wait_weight'  : 1.0,
    # 'bike_wait_weight' : 0.8228,   # down-weights bike by 1.2x
    # 'ped_wait_weight'  : 250.4704,
},

--- What this fixes ---
Current norm_wait=224 → typical delta (482) / 224 = 2.1x clip limit.
Every reward is clipped to ±3.0; the agent sees only a binary signal.

New norm_wait=481.5 → typical delta normalises to ~1.0 before clipping.
clip_wait=3.0 keeps the ±4 headroom for outlier steps.

If you want to reduce bike dominance, use Option B weights.
Bike currently contributes 55% of combined wait;
balanced weights bring it to ~33%.

============================================================